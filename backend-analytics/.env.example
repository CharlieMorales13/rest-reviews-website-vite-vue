# Database Connection (Direct or Pooled)
# Note: Python SQLAlchemy handles connection strings without pgbouncer=true query param
DATABASE_URL="postgresql://user:password@host:port/dbname"

# ML backend — "transformer" uses RoBERTuito (default), "tfidf" uses lightweight TF-IDF model
MODEL_BACKEND=transformer
